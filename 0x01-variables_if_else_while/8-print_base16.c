#include <stdio.h>

/**
 * main - entry point
 *
 * Description: print alphabet
 *
 * Return: 0 (seccess)
*/

int main(void)
{
	int  alpha = '0';

	char ti = 'a';

	while (alpha <= '9')
	{
		putchar(alpha);
		alpha++;
	}
	while (ti <= f)
	{
		putchar(ti);
		ti++;
	}
	putchar('\n');
	return (0);
}
